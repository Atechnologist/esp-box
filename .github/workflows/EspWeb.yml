name: ESP32-S3-BOX CLEAN DUAL MODE (STA + AP + WebUI + RainMaker)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: espressif/idf:release-v5.1

    steps:
      - name: Checkout ESP-BOX
        uses: actions/checkout@v4
        with:
          repository: espressif/esp-box
          submodules: recursive
          fetch-depth: 0

      - name: Fix Git Safe Dir
        run: git config --global --add safe.directory '*'

      - name: Enable Component Manager
        run: echo "IDF_COMPONENT_MANAGER=1" >> $GITHUB_ENV

      # ---------------------------
      # SAFE PATCH ONLY (NO LED CHANGES)
      # ---------------------------
      - name: Fix broken touch dependencies
        run: |
          find . -name "idf_component.yml" -exec sed -i '/esp-lcd-touch/d' {} \; || true
          find . -name "idf_component.yml" -exec sed -i '/esp-lcd-ili9341/d' {} \; || true

      # ---------------------------
      # AP FALLBACK (SAFE ADDITION)
      # ---------------------------
      - name: Add AP Fallback (SAFE)
        run: |
          cat > examples/factory_demo/main/ap_fallback.c << 'EOF'
          #include "esp_wifi.h"
          #include "esp_log.h"

          static const char *TAG = "AP_FALLBACK";

          void start_ap_fallback(void)
          {
              wifi_config_t ap_config = {
                  .ap = {
                      .ssid = "ESP32-BOX-SETUP",
                      .ssid_len = 0,
                      .channel = 6,
                      .password = "12345678",
                      .max_connection = 4,
                      .authmode = WIFI_AUTH_WPA_WPA2_PSK
                  }
              };

              esp_wifi_set_mode(WIFI_MODE_AP);
              esp_wifi_set_config(WIFI_IF_AP, &ap_config);
              esp_wifi_start();

              ESP_LOGI(TAG, "AP Fallback Started");
          }
          EOF

      # ---------------------------
      # WEB UI (SAFE VERSION - NO LED SYMBOLS)
      # ---------------------------
      - name: Add Web UI SAFE
        run: |
          cat > examples/factory_demo/main/web_server.c << 'EOF'
          #include "esp_http_server.h"
          #include "esp_log.h"
          #include "esp_rmaker_core.h"
          #include "esp_rmaker_standard_params.h"

          static const char *TAG = "WEB";

          static esp_err_t root_handler(httpd_req_t *req)
          {
              const char html[] =
              "<html><body style='text-align:center;'>"
              "<h1>ESP32-S3-BOX</h1>"
              "<button onclick=\"fetch('/toggle')\">Toggle</button>"
              "</body></html>";

              httpd_resp_send(req, html, HTTPD_RESP_USE_STRLEN);
              return ESP_OK;
          }

          static esp_err_t toggle_handler(httpd_req_t *req)
          {
              // SAFE LED CONTROL (DO NOT CHANGE SYMBOL)
              extern void app_pwm_led_set_power(bool on);
              static bool state = false;

              state = !state;
              app_pwm_led_set_power(state);

              httpd_resp_send(req, "OK", HTTPD_RESP_USE_STRLEN);
              return ESP_OK;
          }

          void web_server_start(void)
          {
              httpd_config_t config = HTTPD_DEFAULT_CONFIG();
              httpd_handle_t server = NULL;

              if (httpd_start(&server, &config) == ESP_OK) {

                  httpd_uri_t root = {
                      .uri = "/",
                      .method = HTTP_GET,
                      .handler = root_handler
                  };

                  httpd_uri_t toggle = {
                      .uri = "/toggle",
                      .method = HTTP_GET,
                      .handler = toggle_handler
                  };

                  httpd_register_uri_handler(server, &root);
                  httpd_register_uri_handler(server, &toggle);

                  ESP_LOGI(TAG, "WebUI started");
              }
          }
          EOF

      # ---------------------------
      # PATCH MAIN (SAFE INJECTION ONLY)
      # ---------------------------
      - name: Patch main.c safely
        run: |
          FILE=examples/factory_demo/main/main.c

          grep -q web_server.h $FILE || \
          sed -i '/#include "app_rmaker.h"/a #include "web_server.h"\n#include "ap_fallback.c"' $FILE

          sed -i '/web_server_start/d' $FILE

          sed -i '/app_rmaker_start()/a \
          vTaskDelay(pdMS_TO_TICKS(4000));\
          web_server_start();' $FILE

      # ---------------------------
      # BUILD
      # ---------------------------
      - name: Build firmware
        run: |
          . $IDF_PATH/export.sh
          cd examples/factory_demo
          idf.py set-target esp32s3
          cp sdkconfig.ci.box sdkconfig
          idf.py reconfigure
          idf.py build

      # ---------------------------
      # MERGE BIN
      # ---------------------------
      - name: Merge firmware
        run: |
          . $IDF_PATH/export.sh
          cd examples/factory_demo/build

          python $IDF_PATH/components/esptool_py/esptool/esptool.py \
            --chip esp32s3 merge_bin \
            -o factory_merged.bin \
            --flash_mode dio \
            --flash_size 16MB \
            $(cat flash_args)

      # ---------------------------
      # UPLOAD
      # ---------------------------
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: esp32s3-clean-dual-mode
          path: examples/factory_demo/build/factory_merged.bin
