name: ESP32-S3-BOX Stable Dual Mode (WebUI + RainMaker + AP Fallback)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: espressif/idf:release-v5.1

    steps:
    - name: Checkout ESP-BOX
      uses: actions/checkout@v4
      with:
        repository: espressif/esp-box
        submodules: recursive
        fetch-depth: 0

    - name: Fix Git safe directory
      run: git config --global --add safe.directory '*'

    # -------------------------------------------------
    # WEB UI (SAFE FILE GENERATION - NO YAML BREAKS)
    # -------------------------------------------------
    - name: Add Web UI (stable)
      run: |
        mkdir -p examples/factory_demo/main

        cat > examples/factory_demo/main/web_server.h << 'EOF'
        #pragma once
        void web_server_start(void);
        void web_server_start_ap(void);
        EOF

        cat > examples/factory_demo/main/web_server.c << 'EOF'
        #include "web_server.h"
        #include "esp_http_server.h"
        #include "esp_log.h"
        #include "app_pwm_led.h"

        static const char *TAG = "WEB";
        static bool power = false;

        static esp_err_t status_handler(httpd_req_t *req)
        {
            char buf[64];
            snprintf(buf, sizeof(buf),
                "{\"power\":%s}", power ? "true" : "false");

            httpd_resp_set_type(req, "application/json");
            httpd_resp_send(req, buf, HTTPD_RESP_USE_STRLEN);
            return ESP_OK;
        }

        static esp_err_t toggle_handler(httpd_req_t *req)
        {
            power = !power;
            app_pwm_led_set_power(power);

            ESP_LOGI(TAG, "toggle=%d", power);
            httpd_resp_send(req, "OK", HTTPD_RESP_USE_STRLEN);
            return ESP_OK;
        }

        static esp_err_t root_handler(httpd_req_t *req)
        {
            const char html[] =
            "<html><body style='text-align:center;font-family:sans-serif;'>"
            "<h1>ESP32-S3-BOX</h1>"
            "<h2 id='s'>...</h2>"
            "<button onclick='t()'>Toggle</button>"
            "<script>"
            "async function u(){let r=await fetch('/status');let j=await r.json();"
            "document.getElementById('s').innerText=j.power?'ON':'OFF';}"
            "async function t(){await fetch('/toggle');u();}"
            "setInterval(u,500);u();"
            "</script></body></html>";

            httpd_resp_send(req, html, HTTPD_RESP_USE_STRLEN);
            return ESP_OK;
        }

        void web_server_start(void)
        {
            httpd_config_t config = HTTPD_DEFAULT_CONFIG();
            httpd_handle_t server = NULL;

            if (httpd_start(&server, &config) == ESP_OK) {

                httpd_uri_t r = {.uri="/", .method=HTTP_GET, .handler=root_handler};
                httpd_uri_t s = {.uri="/status", .method=HTTP_GET, .handler=status_handler};
                httpd_uri_t t = {.uri="/toggle", .method=HTTP_GET, .handler=toggle_handler};

                httpd_register_uri_handler(server, &r);
                httpd_register_uri_handler(server, &s);
                httpd_register_uri_handler(server, &t);

                ESP_LOGI(TAG, "WebUI started");
            }
        }

        void web_server_start_ap(void)
        {
            web_server_start();
            ESP_LOGW(TAG, "WebUI AP mode fallback active");
        }
        EOF

    # -------------------------------------------------
    # MAIN PATCH (SAFE ENTRY POINT ONLY)
    # -------------------------------------------------
    - name: Patch main boot (minimal safe hook)
      run: |
        FILE=examples/factory_demo/main/main.c

        grep -q web_server.h $FILE || \
        sed -i '/#include "app_rmaker.h"/a #include "web_server.h"' $FILE

        # safe hook AFTER RainMaker init
        sed -i '/app_rmaker_start()/a \
        vTaskDelay(pdMS_TO_TICKS(3000)); \
        web_server_start();' $FILE

    # -------------------------------------------------
    # BUILD
    # -------------------------------------------------
    - name: Build firmware
      run: |
        . $IDF_PATH/export.sh
        cd examples/factory_demo
        idf.py set-target esp32s3
        cp sdkconfig.ci.box sdkconfig
        idf.py reconfigure
        idf.py build

    # -------------------------------------------------
    # MERGE BIN
    # -------------------------------------------------
    - name: Merge firmware
      run: |
        . $IDF_PATH/export.sh
        cd examples/factory_demo/build

        python $IDF_PATH/components/esptool_py/esptool/esptool.py \
          --chip esp32s3 merge_bin \
          -o factory_clean.bin \
          --flash_mode dio \
          --flash_size 16MB \
          $(cat flash_args)

    # -------------------------------------------------
    # UPLOAD
    # -------------------------------------------------
    - name: Upload firmware
      uses: actions/upload-artifact@v4
      with:
        name: esp32s3-box-clean
        path: examples/factory_demo/build/factory_clean.bin
