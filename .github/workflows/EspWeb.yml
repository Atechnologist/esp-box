name: ESP32-S3-BOX Real-Time Web UI (No Reload)

on:
  workflow_dispatch:
  push:
    branches: [ main ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: espressif/idf:release-v5.1

    steps:
      # ----------------------------
      # Checkout ESP-BOX
      # ----------------------------
      - name: Checkout ESP-BOX
        uses: actions/checkout@v4
        with:
          repository: espressif/esp-box
          submodules: recursive
          fetch-depth: 0

      # ----------------------------
      # Fix Git
      # ----------------------------
      - name: Fix Git Safe Directory
        run: git config --global --add safe.directory '*'

      # ----------------------------
      # Enable Component Manager
      # ----------------------------
      - name: Enable Component Manager
        run: echo "IDF_COMPONENT_MANAGER=1" >> $GITHUB_ENV

      # ----------------------------
      # Patch dependencies
      # ----------------------------
      - name: Patch dependencies
        run: |
          find . -name "idf_component.yml" -exec sed -i '/esp-lcd-touch/d' {} \; || true
          find . -name "idf_component.yml" -exec sed -i '/esp-lcd-ili9341/d' {} \; || true

      # ----------------------------
      # Add Web UI (REAL-TIME AJAX)
      # ----------------------------
      - name: Add Web UI
        run: |
          cd examples/factory_demo/main

          cat > web_server.h << 'EOF'
          #pragma once
          void web_server_start(void);
          EOF

          cat > web_server.c << 'EOF'
          #include "web_server.h"
          #include "esp_http_server.h"
          #include "esp_log.h"

          #include "esp_rmaker_core.h"
          #include "esp_rmaker_standard_params.h"

          static const char *TAG = "WEB";

          static esp_rmaker_device_t *light_device = NULL;

          static esp_rmaker_param_t* get_power_param()
          {
              if (!light_device) {
                  const esp_rmaker_node_t *node = esp_rmaker_get_node();
                  if (!node) return NULL;

                  light_device = esp_rmaker_node_get_device_by_name((esp_rmaker_node_t *)node, "Light");
              }

              if (!light_device) return NULL;

              return esp_rmaker_device_get_param_by_name(
                  light_device,
                  ESP_RMAKER_DEF_POWER_NAME
              );
          }

          static bool get_light_state()
          {
              esp_rmaker_param_t *param = get_power_param();
              if (!param) return false;

              const esp_rmaker_param_val_t *val = esp_rmaker_param_get_val(param);
              if (!val) return false;

              return val->val.b;
          }

          // -------- JSON API --------
          static esp_err_t status_handler(httpd_req_t *req)
          {
              char resp[64];
              snprintf(resp, sizeof(resp),
                       "{\"power\":%s}",
                       get_light_state() ? "true" : "false");

              httpd_resp_set_type(req, "application/json");
              httpd_resp_send(req, resp, HTTPD_RESP_USE_STRLEN);
              return ESP_OK;
          }

          static esp_err_t toggle_handler(httpd_req_t *req)
          {
              esp_rmaker_param_t *param = get_power_param();

              if (param) {
                  bool current = get_light_state();

                  esp_rmaker_param_update_and_report(
                      param,
                      esp_rmaker_bool(!current)
                  );

                  ESP_LOGI(TAG, "Toggle (REAL-TIME)");
              }

              httpd_resp_send(req, "OK", HTTPD_RESP_USE_STRLEN);
              return ESP_OK;
          }

          // -------- UI --------
          static esp_err_t root_handler(httpd_req_t *req)
          {
              const char html[] =
              "<html><body style='font-family:sans-serif;text-align:center;'>"
              "<h1>ESP32-S3-BOX</h1>"
              "<h2 id='state'>Loading...</h2>"
              "<button onclick='toggle()' style='font-size:20px;'>Toggle</button>"

              "<script>"
              "async function update(){"
              " let r=await fetch('/status');"
              " let j=await r.json();"
              " document.getElementById('state').innerText = j.power ? 'ON' : 'OFF';"
              "}"
              "async function toggle(){"
              " await fetch('/toggle');"
              "}"
              "setInterval(update,500);"
              "update();"
              "</script>"

              "</body></html>";

              httpd_resp_send(req, html, HTTPD_RESP_USE_STRLEN);
              return ESP_OK;
          }

          void web_server_start(void)
          {
              httpd_config_t config = HTTPD_DEFAULT_CONFIG();
              httpd_handle_t server = NULL;

              if (httpd_start(&server, &config) == ESP_OK) {

                  httpd_uri_t root = {
                      .uri = "/",
                      .method = HTTP_GET,
                      .handler = root_handler
                  };

                  httpd_uri_t toggle = {
                      .uri = "/toggle",
                      .method = HTTP_GET,
                      .handler = toggle_handler
                  };

                  httpd_uri_t status = {
                      .uri = "/status",
                      .method = HTTP_GET,
                      .handler = status_handler
                  };

                  httpd_register_uri_handler(server, &root);
                  httpd_register_uri_handler(server, &toggle);
                  httpd_register_uri_handler(server, &status);

                  ESP_LOGI(TAG, "Web UI REAL-TIME started");
              }
          }
          EOF

      # ----------------------------
      # Patch main.c
      # ----------------------------
      - name: Patch main.c
        run: |
          FILE=examples/factory_demo/main/main.c

          grep -q 'web_server.h' $FILE || sed -i '/#include "app_rmaker.h"/a #include "web_server.h"' $FILE

          sed -i '/web_server_start/d' $FILE

          sed -i '/app_rmaker_start()/a \
          vTaskDelay(pdMS_TO_TICKS(4000));\
          web_server_start();' $FILE

      # ----------------------------
      # Patch CMake
      # ----------------------------
      - name: Patch CMake
        run: |
          FILE=examples/factory_demo/main/CMakeLists.txt

          grep -q web_server.c $FILE || sed -i 's/SRCS "main.c"/SRCS "main.c" "web_server.c"/' $FILE

          grep -q esp_http_server $FILE || sed -i 's/REQUIRES /REQUIRES esp_http_server /' $FILE

      # ----------------------------
      # Build
      # ----------------------------
      - name: Build
        run: |
          . $IDF_PATH/export.sh
          cd examples/factory_demo
          idf.py set-target esp32s3
          cp sdkconfig.ci.box sdkconfig
          idf.py reconfigure
          idf.py build

      # ----------------------------
      # Merge firmware
      # ----------------------------
      - name: Merge firmware
        run: |
          . $IDF_PATH/export.sh
          cd examples/factory_demo/build

          python $IDF_PATH/components/esptool_py/esptool/esptool.py \
            --chip esp32s3 merge_bin \
            -o factory_merged.bin \
            --flash_mode dio \
            --flash_size 16MB \
            $(cat flash_args)

      # ----------------------------
      # Upload firmware
      # ----------------------------
      - name: Upload firmware
        uses: actions/upload-artifact@v4
        with:
          name: esp32s3-box-realtime-web-ui
          path: examples/factory_demo/build/factory_merged.bin
