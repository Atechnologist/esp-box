name: Build ESP32-S3-BOX Factory Demo

on:
  workflow_dispatch:
  push:
    branches: [ master ]

jobs:
  build:
    runs-on: ubuntu-latest

    container:
      image: espressif/idf:release-v5.1

    steps:
      # ---------------------------
      # Checkout repo + submodules
      # ---------------------------
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      # ---------------------------
      # Fix Git safe directory
      # ---------------------------
      - name: Fix Git Safe Directory
        run: |
          git config --global --add safe.directory '*'

      # ---------------------------
      # Build Factory Demo
      # ---------------------------
      - name: Build Factory Demo
        shell: bash
        run: |
          set -e

          . $IDF_PATH/export.sh

          cd examples/factory_demo

          # Target
          idf.py set-target esp32s3

          # Apply config
          cp sdkconfig.ci.box sdkconfig

          # 🔥 CRITICAL FIX (disable registry)
          echo "CONFIG_IDF_COMPONENT_MANAGER=n" >> sdkconfig

          # Clean + build
          idf.py fullclean
          idf.py build

          # Merge using correct layout
          cd build

          esptool.py --chip esp32s3 merge_bin \
            -o factory_demo_box.bin \
            --flash_mode dio \
            --flash_size 16MB \
            $(cat flash_args)

      # ---------------------------
      # Upload firmware
      # ---------------------------
      - name: Upload Firmware
        uses: actions/upload-artifact@v4
        with:
          name: ESP32-S3-BOX-factory-demo
          path: examples/factory_demo/build/factory_demo_box.bin
